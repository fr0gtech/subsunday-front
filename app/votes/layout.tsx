import '../globals.css';

export const metadata = {
  title: 'Sub Sunday - Votes',
  description:
    'Sub Sunday is a weekly event where subscribers to the channel can vote for games theyd like to see Lirik play.Every week, 6 games are selected for the main list and 4 as backups from the list of votes.',
};
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
