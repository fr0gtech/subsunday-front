import '../globals.css';

export const metadata = {
  title: 'Sub Sunday - Leaderboard',
  description: 'Top voted games and top voters by amount of votes and streak. Top 10 for each.',
};
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
